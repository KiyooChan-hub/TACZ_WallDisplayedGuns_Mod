# 仓库迁移记录

日期：2026-09-17。

源仓库：`D:/FORK/MC_codex_Modding`，迁移快照 HEAD：`848d7625fb3deb634824622927876db0ac6e4423`。
目标仓库：`D:/FORK/TACZ_WallDisplayedGuns_Mod`。

## 历史范围

按 `tacz-wall-display-1.20.1/` 与 `tacz-wall-display-1.21.1/` 两个路径筛选并导入 **15 条原始相关提交**。其余工程的文件与无关提交不导入。

保留原始作者、提交者和日期。两条混合提交按装饰枪职责进一步筛选：维护规则提交仅保留子工程约定；测试提交仅保留模型捕获、双向合成、原枪掉落和挂墙渲染，改为通用测试。移除枪包专用配方、换弹、射击、检视和瞄准检查，并同步重写混合提交说明。原始混合提交仍可在源仓库查询。

分离历史改变了父提交和树，提交哈希相应改变。逐条对应关系见 [commit-map.json](migration/commit-map.json)。本次迁移的仓库说明另作为一个新提交记录。保留目标仓库原有的 origin 远程配置，未执行推送。

## 内容与核验

- 两版工程的源码、资源、测试、Gradle Wrapper、发布说明和验证文档全部保留。
- 15 条相关开发历史保留作者和日期；除明确标记的职责拆分及说明修正外，历史内容保持一致。提交映射标记内容及说明被筛选的提交。
- 完整复制并逐文件 SHA-256 校验 **4177 个本地文件，共 658,461,269 字节**。其中开发运行目录、构建输出、缓存、日志继续保留为 Git 忽略文件，不加入提交历史。
- 在新路径强制执行两版单元测试，共 **26 项通过**，并重新编译两版 smoke 测试工具。
- 迁移不会替换玩家实例的模组 JAR、枪包或存档，也不生成新发布版本。

## 源位置与回退资料

完成验证后，原工程目录移至 `E:/GAME/Minecraft/Codex_Output/wall-display-repository-migration-20260917/source-backup/` 保存完整副本，原综合仓库以普通提交记录目录迁出及新位置指引，保留原有历史，不重写其他项目历史。

同一输出目录保存迁移前文件校验清单、筛选后的 fast-export 流、提交标记、测试日志与迁移状态。既有 `E:/GAME/Minecraft/Codex_Output` 发布和诊断资料仍保留原位置。关联枪包测试脚本及活动 classpath 中直接引用旧工程的路径改为新位置；历史环境快照不改写。

## 正确目录核验

最终开发目录为 `D:/FORK/TACZ_WallDisplayedGuns_Mod`（单一目录名）。保留目标仓库原有的 `test.txt` 与远端配置。15 条原始相关开发提交导入 master 分支，可在 Fork 中查看；此前误建仓库的迁移说明提交未导入。本次迁移说明使用正确目录重新提交。
