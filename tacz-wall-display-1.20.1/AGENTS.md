# 本子工程目标

用户明确要求迁移至 Minecraft 1.20.1、Forge 47.4.21，使用 Java 17 字节码。父目录中的 1.21.1 / NeoForge 目标不适用于本子工程。其余提交与验证规范继续遵守。

# 维护与同步规则

后续功能更新默认在 1.21.1 主版本进行。本 1.20.1 Forge 版本仅在用户明确要求时更新，届时以 1.21.1 当前完整功能为基准进行全量同步并验证；此前的移植和部署授权不视为对后续自动同步的授权。
