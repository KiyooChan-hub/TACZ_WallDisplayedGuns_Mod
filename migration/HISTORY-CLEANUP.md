# 迁移历史职责清理（2026-09-17）

原迁移按目录筛选，保留了混合提交中的枪包专用测试及完整提交说明。本轮按用户要求直接清理历史中的对应内容。

## 保留范围

- 15 条装饰枪相关开发提交；作者、提交者及时间保留。
- 混合测试改名为 `DisplayCompatibilitySmoke`，仅测试配件/四档弹匣静态网格、2×2 与 3×3 双向合成、完整原枪掉落、挂墙渲染。
- 默认使用 TACZ 自带的两种枪械；可通过 `wallgun.compatibilityGuns` 指定两个兼容测试配件的原枪 ID，不依赖独立枪包。
- 移除独立枪包配方、换弹、射击、检视、第一人称瞄准等专用检查和入口。旧测试文件在当前可达历史中不存在。
- 修正两条混合提交说明及迁移说明。新旧开发提交对应见 `commit-map.json`；本轮重写前后对应见 `history-cleanup-map.json`。

## 验证

- 1.21.1 的 13 项单元测试通过，拆分后的测试源集编译成功。
- 隔离正式客户端实际进档回归通过：两种原枪 × 四档弹匣外观，两种合成栏的完整数据往返，原枪掉落和两把装配枪挂墙显示。
- 1.20.1 全部工程、1.21.1 产品代码及单元测试目录的 Git 树哈希与重写前完全一致；本轮不重新构建或部署产品 JAR。
- 全部历史核对：作者及日期保持；没有遗留 `LegacyGunSmoke` 文件或旧入口；当前仅 master 分支指向清理后的历史。

## 回退与远端

重写前完整 Git bundle、旧引用清单、测试日志和成功报告保存在：
`E:/GAME/Minecraft/Codex_Output/wall-display-history-cleanup-20260917/`。

已清理本地 origin 的旧跟踪引用和旧 reflog，避免 Fork 本地历史图继续显示旧分支；origin 的 URL 与分支配置保留。未推送或修改远端。

后续已将清理后的完整历史迁移到 GitHub，并独立拉取验证。本地 origin 与 master 上游均已切换到 GitHub，不再使用 Codeup，也不再需要此前针对 Codeup 的强制推送步骤。详见 [GitHub 迁移记录](GITHUB-MIGRATION.md)。
