# 开发规范

## 分支与目标

- 迁移目标固定为 Minecraft 1.21.1、NeoForge 21.1.248、Java 21。
- TACZ 装饰枪模组后续默认仅在 tacz-wall-display-1.21.1 更新功能。仅当用户明确指示更新 1.20.1 时，才将 1.21.1 当前完整功能全量同步至 tacz-wall-display-1.20.1（Forge）；不得将日常功能更新自动同步到 1.20.1。

## 每轮工作流程

1. 开始前检查当前分支、工作树和既有用户改动，明确本轮文件范围。
2. 只修改本轮需求直接涉及的文件，不覆盖或整理无关改动。
3. 使用与本轮风险相称的检查验证改动；验证未通过时不得提交。
4. 提交前检查 `git status` 和暂存区，只暂存本轮对应文件，不得夹带无关文件。
5. 每轮工作完成且验证通过后必须创建一次对应提交。
6. 本轮未明确要求打包时，完成必要验证和提交即可，不额外执行打包或发布。

## Commit 规范

- Commit subject 必须严格使用 `codex自动生成_yyMMddHHmm`。
- 时间使用创建提交时的本地时间，格式为两位年、月、日、时、分。
- Commit description 必须使用以下结构，并可在不改变含义的前提下精简：

```text
用户需求:
- <本轮需求概述>

实际完成:
- <本轮实际完成内容>

验证结果:
- <执行的验证及结果>
```

## Push 与发布限制

- AI 不得自动执行任何 `git push`，包括分支、标签和强制推送。
- Push 必须由用户人工手动完成。
- 未经用户明确要求，不执行打包、发布、上传制品或创建远端发布版本。
